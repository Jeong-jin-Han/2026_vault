---
tags:
  - paper
date: 2026-02-11
---

#paper/title
Breaking Barriers: Expanding GPU Memory with Sub-Two Digit Nanosecond Latency CXL controller

#paper/keywords
CXL, Memory Expansion, GPGPU

#paper/abstract
This work introduces a GPU storage expansion solution utilizing CXL, featuring a novel GPU system design with multiple CXL root ports for integrating diverse sotrage mdia (DRAMs and/or SSDs). We developed and siliconized a custom CXL controller integrated at the hardware RTL level, achieving two-digit nanosecond round trip latency, the first in the field. This study also includes speculative read and deterministic store mechanisms to efficiently manage read and write operations to hide the endpoint's backend media latency variation. Performance evaluations reveal our approach significantly outperforms existing methods, marking a substantial advancement in GPU storage technology.

#paper/figs
### Fig1. A high-levle view of GPU storage expansion
SMs는 무엇인가?
NVM은 무엇인가? non-volatile memory


AIC-level prototype은 무엇인가?
CXL endpoint는 무엇인가?
### Fig2. Data movement
Tiled model parameter가 무엇을 의미하는가?
Target Training Model이 무엇을 의미하는가?

### Fig3. Latency analysis
CXL Trans Layer와 PCIe Trans Layer의 차이점은 무엇인가?
CXL Link Layer와 PCIe Data Link Layer의 차이점은 무엇인가?
Flex Bus Physical Layer는 무엇을 의미하는가?

### Fig4. Sylicon layout

### Fig5. Latency
Round trip Lantency에서 
SMT, TPP, CXL-opt가 무엇을 의미하는가?

### Fig6. GPU architecture
HDM Decoder가 무엇을 의미하는가?
CXL root ocmplex에서 host bridge, CXL RP 0, CXL Ctrl.

GPU(Vortex)에서 Vortex가 무엇을 의미하는가?
SM, LLC, PE가 무엇을 의미하는가?
CXL RC, PCIe ep가 무엇을 의미하는가?
Core에 Firmware가 있는데 이 Firmware의 역할은?

### Fig7. RTL floorplan
floorplan이라는 것은 무엇을 의미하는 것인가?

### Fig8. GPU memory space
memory map 에서 CXL CHBCR은 무엇이고 DMA와 여녈된 이유는 무엇인가?

### Fig9. Speculative read
CXL Root port의 역할은 무엇일까?
SR Queue는 무엇이고, 
CXL Trans Layer는 무엇인가?

speculative read가 무엇인가?

### Fig10. Deterministic store
deterministic store가 무엇을 의미하는가?

### Table1. Setup
Vortex Configuration은 무엇을 의미하고, core와 cluster의 차이
PCIe configuration의 의미
CXL configuration의 의미는 무엇이고, Sync-header bypass는 무엇을 의미하는 것일까?

### Table2. Workloads
workload는 어떤 식으로 정하는 것일까? store ratio의 계산 방식은 어떤 식으로?
BFS, SpMV, GEMM, Conv, VecAddd는 무엇을 의미하는가?

### Fig11. Kernel execution time
normalized execution time이라고 했을 때 어떤 것을 normalized를 한 것일까?

Load intensive, Store intensive한 작업의 예시로 각각 무엇이 있고 이렇게 구분한 목적이 무엇일까?

### Fig12. IPC
IPC가 무엇을 의미하는 것일까? 
instruction per cycle?
CPI값으로 하지 않는 이유는 무엇일까?

### Fig13. Deterministic Store
deterministic store가 무엇인가?


#paper/conclusion
We introduce a method to increase GPU storage using CXL technology, integrating DRAM and/or SSDs through CXL multiple root ports. OUR custom-designed CXL contoller, implemented directly in the hardware , achieves fast response times not seen before in this field. We further refine this system with feau

#paper/summary


#paper/review
# Why read?

# My Commentary


